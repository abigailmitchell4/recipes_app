<br/>
<h4>Add a Note</h4>
<%= form_for [@recipe, @note] do |n| %>
  <%= n.label :body %>
  <%= n.text_area :body, required: true %>
  <br />
  <%= n.label :author %>
  <%= n.text_field :author, placeholder: 'Leave Blank To Remain Anonymous' %>
  <br />
  <%= n.submit class:"swaves-effect waves-light btn-small btn-flat"%>
<% end %>
<%= link_to "Back to Recipe", recipe_path(@recipe), class:"swaves-effect waves-light btn-small btn-flat" %>